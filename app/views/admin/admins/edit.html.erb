<div class="card">
  <div class="card__section card__section--border">
    <h3><%= link_to 'Admins /', admin_admins_path,  class: 'u-text-light u-text-regular' %> <%= @admin.name %></h3>
  </div>
  <div class="card__section card__section--border">
    <%= render partial: 'form', locals: { resource: @admin, url: admin_admin_path(@admin), button_text: "Save changes", method: :put } %>
  </div>
</div>