<% content_for :meta_description do %>
  "<%= summary.present? ? summary : business.description %>"
<% end %>
<% content_for :meta_url do %>
  "<%= root_url + business_path(id: business.slug) %>"
<% end %>
<% content_for :meta_image do %>
  "<%= image_url(business.banner.url(:small)) %>"
<% end %>
