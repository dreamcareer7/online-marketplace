<%= content_for :sub_nav do %>
  <% if ["project_steps", "businesses"].include?(controller_name) || ["projects"].include?(controller_name) && action_name == 'edit' || ["users"].include?(controller_name) && action_name == 'edit' %>
    <%= yield :section_header %>
  <% else %>
    <%= render partial: "partials/nav/profile_nav" %>
  <% end %>
<% end %>

<%= render template: "layouts/user_dashboard" %>
