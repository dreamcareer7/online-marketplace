$('#required_category').val("<%= @category.id %>");
$('.professionals-form').html("<%=j render partial: 'service_fields', locals: { category: @category, project: @project } %>");
$('.select2').select2();
